# Masive-SSH-Bruter-Payload-Loader
This program brute the ssh from a .txt IP addresses list then execute a configured payload when the machine get bruted.  The code is written in C++ and high optimized.
